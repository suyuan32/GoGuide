import{_ as s,a as o,b as r,c as d,d as c}from"./tcp-congestion-control-I9aAiC2N.js";import{_ as l}from"./plugin-vue_export-helper-DlAUqK2U.js";import{r as h,o as p,c as m,a as e,d as t,b as i,e as n}from"./app-5vvbqJKh.js";const u="/assets/image/article/network/tcp_en.png",g="/assets/image/article/network/tcp_state_en.png",f={},b=n('<h3 id="tcp" tabindex="-1"><a class="header-anchor" href="#tcp"><span>TCP</span></a></h3><p>Transmission Control Protocol (TCP) is a connection-oriented, reliable, byte-stream-based transport layer communication protocol.</p><h4 id="tcp-header-structure" tabindex="-1"><a class="header-anchor" href="#tcp-header-structure"><span>TCP Header Structure</span></a></h4><figure><img src="'+u+'" alt="tcp header" tabindex="0" loading="lazy"><figcaption>tcp header</figcaption></figure><table><thead><tr><th>TCP Header Field</th><th>Description</th></tr></thead><tbody><tr><td>Source Port</td><td>Sender&#39;s port number, range (0~65535)</td></tr><tr><td>Destination Port</td><td>Receiver&#39;s port number, range (0~65535)</td></tr><tr><td>Sequence Number</td><td>If the TCP data is too large (greater than the allowable degree of IP packet), it needs to be segmented. This sequence number records the sequence number of each packet, allowing the receiver to reassemble the TCP data. The value of the sequence number field refers to the sequence number of the first byte of data sent by this segment. Abbreviated as <code>seq</code></td></tr><tr><td>Acknowledgment Number</td><td>To confirm that the receiver has indeed received the packet data sent out by the sender, when the sender receives this acknowledgment code, it can confirm that the previous packet has been correctly received. This acknowledgment number is the sequence number of the first byte of data from the next segment expected to be received from the other party. Abbreviated as <code>ack</code></td></tr><tr><td>Header Length</td><td>Indicates the length of the TCP header, range (0~15), unit 32bit, for example, when the value is 5, it means that the header length is 20Byte(160bit), if the option field is empty, then the TCP header length is 20Byte, that is, the value is 5</td></tr><tr><td>URG</td><td><code>URG=1</code> indicates that there is urgent data, the last byte of the urgent data is pointed out by the urgent data pointer, generally less used</td></tr><tr><td>ACK</td><td><code>ACK=1</code> indicates that the value in the acknowledgment number field is valid, 0 indicates invalid</td></tr><tr><td>PSH</td><td><code>URG=1</code> indicates that the urgent pointer field is valid, representing that this packet is an urgent packet. It tells the system that there is urgent data in this segment and should be transmitted as soon as possible (equivalent to high-priority data)</td></tr><tr><td>RST</td><td><code>RSP=1</code> rebuilds the connection, if the RST bit is received, some errors usually occur</td></tr><tr><td>SYN</td><td><code>SYN=1</code> indicates that this is a connection request or connection acceptance message, generally used in the handshake stage</td></tr><tr><td>FIN</td><td><code>FIN=1</code> indicates that the data of the sender of this segment has been sent and requests to release the transport connection</td></tr><tr><td>Receive Window</td><td>Used for flow control, indicating the number of bytes the receiver is willing to receive, range <code>0~65535</code> bytes</td></tr><tr><td>Checksum</td><td>The checksum covers the entire TCP segment, i.e., the TCP header and TCP data. This is a mandatory field, it must be calculated and stored by the sender, and verified by the receiver</td></tr><tr><td>Urgent Data Pointer</td><td>When <code>URG=1</code> is valid, it is a positive offset, and the sum with the value in the sequence number represents the sequence number of the last byte of urgent data</td></tr><tr><td>Options</td><td>Used when the sender and receiver negotiate the maximum segment length (MSS) (only exists in SYN messages) or used as a window adjustment factor in high-speed network environments, can also store timestamp data</td></tr></tbody></table><h4 id="tcp-state-machine" tabindex="-1"><a class="header-anchor" href="#tcp-state-machine"><span>TCP State Machine</span></a></h4><figure><img src="'+g+'" alt="TCP Stateful" tabindex="0" loading="lazy"><figcaption>TCP Stateful</figcaption></figure><h4 id="tcp-three-way-handshake" tabindex="-1"><a class="header-anchor" href="#tcp-three-way-handshake"><span>TCP Three-Way Handshake</span></a></h4><figure><img src="'+s+'" alt="TCP Connection" tabindex="0" loading="lazy"><figcaption>TCP Connection</figcaption></figure><div class="hint-container tip"><p class="hint-container-title">Three-Way Handshake Process</p><ol><li>The client sends <code>SYN=1</code>, and specifies the client&#39;s initial sequence number <code>ISN</code>, i.e., <code>x</code>.</li><li>The server sends its own <code>SYN</code> segment as an acknowledgment, also specifying its own <code>ISN</code>, i.e., <code>y</code>. To acknowledge the client&#39;s <code>SYN</code>, it sets <code>ACK</code> to <code>x+1</code>. In this way, each time a <code>SYN</code> is sent, the sequence number will increase by 1. If there is a loss, it will be retransmitted.</li><li>To acknowledge the server&#39;s <code>SYN</code>, the client sets <code>ACK</code> to <code>y+1</code> in the returned value.</li></ol></div><div class="hint-container tip"><p class="hint-container-title">TCP Four-Way Handshake</p><ol><li>The client sends <code>FIN=1</code>, and includes its current sequence number <code>x+2</code>. It also includes an <code>ACK=y+1</code> to acknowledge the data most recently received from the other side.</li><li>The server adds 1 to the value <code>x+2</code> as the <code>ACK</code> sequence number, indicating that the previous packet has been received. At this time, the upper-layer application will be notified that the other end has initiated a shutdown operation, which usually causes the application to initiate its own shutdown operation.</li><li>The server initiates its own <code>FIN=1</code>, <code>seq=y+1</code>.</li><li>The client acknowledges, sends <code>ACK=y+2</code> to the server.</li></ol></div><div class="hint-container warning"><p class="hint-container-title">Why is a three-way handshake needed? What is its purpose?</p><details class="hint-container details"><summary>Details</summary><p>The process of TCP establishing a connection is the process of synchronizing sequence numbers, SYN (Synchronize Sequence Numbers) is to synchronize sequence numbers. Therefore, the purpose of the three-way handshake is to allow the client (Client) and the server (Service) to obtain each other&#39;s sequence number.</p></details></div><div class="hint-container warning"><p class="hint-container-title">Why is a four-way handshake needed? What is its purpose?</p><details class="hint-container details"><summary>Details</summary><p>The reason why a four-way handshake is needed is because TCP is a <strong>full-duplex</strong> protocol, i.e., both the client and the server can actively send messages, so both ends need to send disconnect instructions after the transmission is completed, and need to send <code>FIN=1</code> separately to disconnect, and use <code>ACK</code> to determine whether the sending was successful.</p></details></div><div class="hint-container caution"><p class="hint-container-title">What happens if SYN times out during connection?</p><details class="hint-container details"><summary>Details</summary><p>If the client sends a <code>SYN</code> command, and drops the line before the server returns the <code>SYN</code> command, the server will try to resend the <code>SYN-ACK</code> command. Under Linux, the default is to retry 5 times, the interval time starts to double from 1s, i.e., <code>1s, 2s, 4s, 8s, 16s</code>, so the timeout time is <code>1s + 2s + 4s+ 8s+ 16s + 32s = 63s</code>. After the timeout, TCP will disconnect.</p></details></div><h4 id="how-does-tcp-ensure-the-reliability-of-transmission" tabindex="-1"><a class="header-anchor" href="#how-does-tcp-ensure-the-reliability-of-transmission"><span>How does TCP ensure the reliability of transmission?</span></a></h4><div class="hint-container tip"><p class="hint-container-title">TCP ensures the reliability of data transmission through the following features</p><ul><li>Sequence numbers and acknowledgments</li><li>Timeout retransmission</li><li>Flow control</li><li>Congestion control</li><li>Checksum</li></ul></div><h5 id="sequence-numbers-and-acknowledgment-signals" tabindex="-1"><a class="header-anchor" href="#sequence-numbers-and-acknowledgment-signals"><span><strong>Sequence numbers and acknowledgment signals</strong></span></a></h5><p>TCP uses sequence numbers to sort and deduplicate packets, and through the ACK acknowledgment mechanism, it ensures that packets are successfully delivered, ensuring the integrity of the data.</p><h5 id="timeout-retransmission" tabindex="-1"><a class="header-anchor" href="#timeout-retransmission"><span><strong>Timeout retransmission</strong></span></a></h5><p>TCP can retransmit packets until an ACK acknowledgment is received when packets are lost or delayed, through the timeout retransmission mechanism.</p><div class="hint-container warning"><p class="hint-container-title">What is RTT? What is RTO?</p><details class="hint-container details"><summary>Details</summary><p>RTT stands for Round Trip Time, which refers to the time required for data to be transmitted from the sender to the receiver and back. RTT is used to measure network latency, that is, the total time that data passes through during transmission.</p><p>RTO stands for Retransmission Timeout, which refers to the time that the sender waits for acknowledgment (ACK) after sending data in network communication. If the sender does not receive an acknowledgment within the RTO time, it will assume that the data is lost and resend the data.</p><p>The calculation of RTO is usually based on the measurement of RTT. The sender estimates the RTO of the next data transmission based on the previous RTT. In general, the value of RTO will be larger than RTT to ensure that data can be successfully retransmitted even in cases of high or unstable network latency.</p><p>The calculation method of RTO can vary depending on the specific network protocol or implementation, but its purpose is to ensure the reliable transmission of data to cope with problems such as packet loss, delay, and congestion in the network.</p></details></div><div class="hint-container warning"><p class="hint-container-title">What is the impact of RTO length on retransmission?</p><details class="hint-container details"><summary>Details</summary><p>If the RTO is too long, the retransmission time will be greatly extended, reducing transmission efficiency. If the RTO is too short, it may lead to frequent retransmissions, exacerbating network congestion, and further triggering more retransmissions.</p></details></div>',22),y={class:"hint-container tip"},w=e("p",{class:"hint-container-title"},"Common retransmission mechanisms",-1),v={class:"hint-container details"},k=n('<summary>Details</summary><ul><li>Timeout retransmission</li></ul><figure><img src="'+o+'" alt="timeout retransmission" tabindex="0" loading="lazy"><figcaption>timeout retransmission</figcaption></figure><p>There are two situations for timeout retransmission, the timeout caused by the loss of the sent data packet, and the timeout caused by the loss of the returned ACK data packet. Under the timeout retransmission mechanism, a timer is started every time a data packet is sent. If the timer expires, retransmission will be triggered. If the retransmission fails, the next timeout time will be twice the current value. <mark>The disadvantage of timeout retransmission is that the cycle is long, which may reduce efficiency.</mark></p><ul><li>Fast retransmission</li></ul><figure><img src="'+r+'" alt="tcp" tabindex="0" loading="lazy"><figcaption>tcp</figcaption></figure><p>Under the fast retransmission mechanism, after a data packet is lost, the receiving end immediately returns a duplicate acknowledgment packet for each out-of-order packet received, informing the sending end of the missing packet. When the sender receives three duplicate acknowledgment packets, it will immediately retransmit the missing packet.</p><p><mark>Under the fast retransmission mechanism, we can see that the packets <code>3,4,5</code> all return <code>ACK=2</code>, so we need to retransmit the packets <code>3,4,5</code>. Is there a way to not need to retransmit packets <code>3,4,5</code>?</mark></p><ul><li>SACK (Selective Acknowledgment)</li></ul><figure><img src="'+d+'" alt="SACK" tabindex="0" loading="lazy"><figcaption>SACK</figcaption></figure>',10),T={href:"https://www.ietf.org/rfc/rfc2883.txt",target:"_blank",rel:"noopener noreferrer"},C=e("ul",null,[e("li",null,"D-SACK (Duplicate SACK)")],-1),P={href:"https://datatracker.ietf.org/doc/html/rfc2018",target:"_blank",rel:"noopener noreferrer"},S=n('<h5 id="flow-control" tabindex="-1"><a class="header-anchor" href="#flow-control"><span><strong>Flow Control</strong></span></a></h5><p>TCP uses a sliding window to control traffic, allowing the sender to control the speed of data transmission based on the receiver&#39;s receiving capacity.</p><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>The receiver will write the currently available window size into the TCP header when responding with <code>ACK</code>, and the sender adjusts the transmission rate based on the window size.</p></div><h5 id="congestion-control" tabindex="-1"><a class="header-anchor" href="#congestion-control"><span><strong>Congestion Control</strong></span></a></h5><div class="hint-container tip"><p class="hint-container-title">Algorithms</p><ul><li>Slow Start</li><li>Congestion Avoidance</li><li>Fast Retransmit</li><li>Fast Recovery</li></ul></div><figure><img src="'+c+'" alt="Congestion Control" tabindex="0" loading="lazy"><figcaption>Congestion Control</figcaption></figure><div class="hint-container info"><p class="hint-container-title">Related Terms</p><ul><li>cwnd - Congestion Window</li><li>ssthresh - Slow Start Threshold</li></ul></div><div class="hint-container important"><p class="hint-container-title">Important</p><ul><li><p>If <code>cwnd &lt; ssthresh</code>, use the Slow Start algorithm.</p></li><li><p>If <code>cwnd &gt; ssthresh</code>, use the Congestion Avoidance algorithm.</p></li><li><p>If <code>cwnd = ssthresh</code>, use either the Slow Start or Congestion Avoidance algorithm.</p></li></ul></div><h6 id="slow-start" tabindex="-1"><a class="header-anchor" href="#slow-start"><span><strong>Slow Start</strong></span></a></h6><p>In the Slow Start phase, the initial value of <code>cwnd</code> is <code>1</code>, and <code>cwnd</code> will double every time a propagation round passes, until it reaches <code>ssthresh</code>.</p><h6 id="congestion-avoidance" tabindex="-1"><a class="header-anchor" href="#congestion-avoidance"><span><strong>Congestion Avoidance</strong></span></a></h6><p>When <code>cwnd</code> reaches <code>ssthresh</code>, the Congestion Avoidance algorithm will be executed. At this time, the growth of <code>cwnd</code> changes from doubling to adding <code>1</code> every time a propagation round passes. If the sender detects network congestion (i.e., the sent message does not get a timely response), it will set <code>ssthresh</code> to half of the <code>cwnd</code> value when network congestion occurs, and the value of <code>cwnd</code> will be reset to <code>1</code>, and Slow Start will be re-executed.</p><h6 id="fast-retransmit-and-fast-recovery" tabindex="-1"><a class="header-anchor" href="#fast-retransmit-and-fast-recovery"><span><strong>Fast Retransmit and Fast Recovery</strong></span></a></h6><p>Fast Retransmit has been introduced earlier, that is, if three consecutive duplicate acknowledgments are received, the yet-to-be-received packet segment will be sent immediately. The Fast Recovery algorithm needs to be used in conjunction with the Fast Retransmit algorithm.</p><div class="hint-container info"><p class="hint-container-title">Fast Recovery Algorithm</p><ul><li>When the sender receives three consecutive duplicate acknowledgments, it halves <code>ssthresh</code>.</li><li>Set <code>cwnd</code> to the size of <code>ssthresh</code></li></ul></div><h5 id="checksum" tabindex="-1"><a class="header-anchor" href="#checksum"><span><strong>Checksum</strong></span></a></h5><p>TCP will also calculate a checksum for the data to ensure that the data is not lost or erroneous during transmission.</p><h3 id="udp" tabindex="-1"><a class="header-anchor" href="#udp"><span>UDP</span></a></h3><p>UDP, short for User Datagram Protocol, is a connectionless transport layer protocol in the OSI (Open Systems Interconnection) reference model. It provides a simple, unreliable transaction-oriented message delivery service, and its official specification is <code>IETF RFC 768</code>.</p><h4 id="characteristics" tabindex="-1"><a class="header-anchor" href="#characteristics"><span>Characteristics</span></a></h4><ul><li>Connectionless: UDP does not need to establish a connection between the client and the server before transmitting data.</li><li>Fast: Since UDP does not need to handshake or check whether the data has arrived correctly, it can transmit data faster than TCP.</li><li>Unreliability: Once the datagram is sent, it is impossible to know whether it has arrived safely and completely. If the UDP datagram is lost during transmission, it will not be resent.</li><li>Widely used: UDP is used to support network applications that need to transmit data between computers. Many client/server model network applications, including network video conferencing systems, need to use the UDP protocol. For example, many Internet phone services use IP voice (VoIP) usually sent using UDP.</li></ul><h4 id="structure" tabindex="-1"><a class="header-anchor" href="#structure"><span>Structure</span></a></h4><table><thead><tr><th>Field</th><th>Size</th><th>Description</th></tr></thead><tbody><tr><td>Source Port</td><td>16-bit</td><td>Source port number. If all are 0, no reply is allowed</td></tr><tr><td>Destination Port</td><td>16-bit</td><td>Destination port number</td></tr><tr><td>Length</td><td>16-bit</td><td>Length of the UDP user datagram, its minimum value is 8 (header only)</td></tr><tr><td>Checksum</td><td>16-bit</td><td>Detect whether the data is lost or modified during transmission</td></tr></tbody></table><h4 id="broadcast-types" tabindex="-1"><a class="header-anchor" href="#broadcast-types"><span>Broadcast Types</span></a></h4><table><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td>Unicast</td><td>Used for end-to-end communication between two hosts. That is, one-to-one (client and server point-to-point connection)</td></tr><tr><td>Broadcast</td><td>Used for a host to communicate with all hosts on the entire LAN. That is, one to all. Broadcasting is prohibited from transmitting on the Internet broadband network (broadcast storm)</td></tr><tr><td>Multicast</td><td>Communicate with a specific group of hosts, not all hosts on the entire LAN. That is, one to a group</td></tr></tbody></table><div class="hint-container warning"><p class="hint-container-title">Warning</p><ul><li>TCP only supports one-to-one</li><li>UDP multicast is more commonly used, broadcast is only used for LAN</li></ul></div><div class="hint-container info"><p class="hint-container-title">Difference between TCP and UDP</p><table><thead><tr><th>Feature</th><th>TCP</th><th>UDP</th></tr></thead><tbody><tr><td>Reliable Transmission</td><td>Yes</td><td>No</td></tr><tr><td>Connection</td><td>Connection-oriented</td><td>Connectionless</td></tr><tr><td>Data Orderliness</td><td>Yes</td><td>Not guaranteed</td></tr><tr><td>Data Boundary</td><td>Not preserved</td><td>Preserved</td></tr><tr><td>Transmission Speed</td><td>Relatively slow</td><td>Fast</td></tr><tr><td>Flow Control and Congestion Control</td><td>Yes</td><td>No</td></tr><tr><td>Protocol Type</td><td>Heavyweight</td><td>Lightweight</td></tr><tr><td>Header Length</td><td>20 bytes</td><td>8 bytes</td></tr></tbody></table></div><h3 id="common-tcp-udp-attacks" tabindex="-1"><a class="header-anchor" href="#common-tcp-udp-attacks"><span>Common TCP/UDP Attacks</span></a></h3><div class="hint-container warning"><p class="hint-container-title">SYN Flood Attack</p><p>The principle of a SYN Flood attack is very simple. It takes advantage of the server&#39;s timeout mechanism. When the receiver receives a <code>SYN</code> request, it responds with a <code>SYN + ACK</code> reply. If the receiver doesn&#39;t receive a response to its reply, it retries the connection (learn more about what happens when SYN times out in the TCP three-way handshake and four-way handshake). This creates a half-open connection state. An attacker can exploit this by sending a large number of half-open connection requests from multiple hosts to the server, eventually causing the server&#39;s resources to be exhausted and unable to provide services.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><strong>How to detect SYN Flood attacks?</strong></p><ol><li>Increased inbound network traffic: SYN Flood causes a sudden increase in inbound network traffic to the server.</li><li>Decreased network performance: SYN Flood leads to a decline in network performance.</li><li>Numerous SYN half-open connections: SYN Flood generates a large number of half-open connections.</li><li>Unresponsive server: SYN Flood prevents the server from responding to requests.</li></ol></div><div class="hint-container important"><p class="hint-container-title">How to mitigate?</p><table><thead><tr><th>Defense Measure</th><th>Description</th></tr></thead><tbody><tr><td>Increase maximum half-open connections</td><td>Increasing the maximum number of half-open connections allows the server to handle a large number of incoming SYN requests, providing a buffer against flood attempts.</td></tr><tr><td>Filtering</td><td>Set up rules in network devices to identify and block malicious SYN requests based on specific patterns or known malicious IP addresses.</td></tr><tr><td>Reduce SYN-RECEIVED timer</td><td>By reducing the time the server waits for an ACK response after sending SYN-ACK, resources allocated to half-open connections can be released more quickly.</td></tr><tr><td>SYN cache</td><td>The server uses a cache to store minimal information about each request, minimizing resource consumption.</td></tr><tr><td>SYN Cookies</td><td>The server responds with a SYN-ACK without immediately allocating resources for the connection. Resources are allocated only upon receiving a legitimate ACK response.</td></tr><tr><td>Load balancer</td><td>Distribute incoming network traffic across multiple servers, ensuring that no single server becomes the primary target of the attack.</td></tr><tr><td>Firewall and proxy</td><td>Deploying firewalls and proxies can filter out malicious traffic before it reaches the target server, providing an additional layer of defense.</td></tr><tr><td>Honeypots and honeynets</td><td>Honeypots absorb and divert attacks, providing valuable distractions to attackers and protecting critical infrastructure. Honeynets gather threat intelligence by collecting data on attacker strategies.</td></tr><tr><td>Rate limiting</td><td>Limiting the rate of incoming connection requests is an important strategy to prevent SYN flood attacks.</td></tr><tr><td>Hybrid approaches</td><td>Combining multiple techniques can create layered defenses against TCP SYN Flood attacks, ensuring that protection remains even if one method fails.</td></tr><tr><td>Cloud-based DDoS protection solutions</td><td>Utilizing cloud-based DDoS protection solutions can offload and filter traffic to dedicated platforms capable of handling large-scale attacks.</td></tr></tbody></table></div></div><div class="hint-container warning"><p class="hint-container-title">What is a UDP Flood Attack?</p><div class="hint-container important"><p class="hint-container-title">Important</p><p>When a server receives a UDP packet, it performs the following operations:</p><ol><li>Checks if any program is listening on the corresponding port.</li><li>If no program is found, the server responds with an ICMP packet indicating the destination is unreachable.</li></ol></div><p>Therefore, an attacker can overwhelm the server&#39;s resources by frequently sending a large number of UDP requests to multiple ports, causing the server to exhaust its resources while handling a large number of responses.</p><details class="hint-container details"><summary>How to mitigate?</summary><p>One way to mitigate UDP Flood attacks is by limiting the ICMP response time. However, this can also impact legitimate PING requests.</p></details></div>',30);function x(_,D){const a=h("ExternalLinkIcon");return p(),m("div",null,[b,e("div",y,[w,e("details",v,[k,e("p",null,[t("The SACK method ("),e("a",T,[t("RFC 2018"),i(a)]),t(") adds a buffer to the tcp option field to record the transmitted packets, so that the sender can see the packets that have not been successfully transmitted, allowing the sender to only transmit the missing packets without needing to retransmit other packets.")]),C,e("p",null,[t("D-SACK ("),e("a",P,[t("RFC 2883"),i(a)]),t(") mainly solves the problem of ACK loss. D-SACK uses the first segment of SACK as a flag to mark the packets that have been ACKed. When the receiving end receives a duplicate packet, it will write the packet into the D-SACK flag, telling the sender that the packet has been received, and the ACK may have been lost.")])])]),S])}const R=l(f,[["render",x],["__file","3-tcp-udp.html.vue"]]),U=JSON.parse('{"path":"/en/guide/concepts/network/3-tcp-udp.html","title":"TCP/UDP","lang":"en-US","frontmatter":{"order":3,"title":"TCP/UDP","icon":"line-md:star-filled","head":[["meta",{"name":"keywords","content":"tcp, udp, network, transmission control protocol, user datagram protocol"}],["link",{"rel":"alternate","hreflang":"zh-cn","href":"https://goguide.ryansu.tech/guide/concepts/network/3-tcp-udp.html"}],["meta",{"property":"og:url","content":"https://goguide.ryansu.tech/en/guide/concepts/network/3-tcp-udp.html"}],["meta",{"property":"og:site_name","content":"Go Guide"}],["meta",{"property":"og:title","content":"TCP/UDP"}],["meta",{"property":"og:description","content":"TCP Transmission Control Protocol (TCP) is a connection-oriented, reliable, byte-stream-based transport layer communication protocol. TCP Header Structure tcp headertcp header T..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://goguide.ryansu.tech/assets/image/article/network/tcp_en.png"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"property":"og:locale:alternate","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2024-05-05T13:24:21.000Z"}],["meta",{"property":"article:author","content":"Go Guide"}],["meta",{"property":"article:modified_time","content":"2024-05-05T13:24:21.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"TCP/UDP\\",\\"image\\":[\\"https://goguide.ryansu.tech/assets/image/article/network/tcp_en.png\\",\\"https://goguide.ryansu.tech/assets/image/article/network/tcp_state_en.png\\",\\"https://goguide.ryansu.tech/assets/image/article/network/tcp-connect.png\\",\\"https://goguide.ryansu.tech/assets/image/article/network/timeout-retransmission.png\\",\\"https://goguide.ryansu.tech/assets/image/article/network/tcp-3-retry-new.png\\",\\"https://goguide.ryansu.tech/assets/image/article/network/sack.png\\",\\"https://goguide.ryansu.tech/assets/image/article/network/tcp-congestion-control.png\\"],\\"dateModified\\":\\"2024-05-05T13:24:21.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Go Guide\\",\\"url\\":\\"https://github.com/suyuan32\\"}]}"]],"description":"TCP Transmission Control Protocol (TCP) is a connection-oriented, reliable, byte-stream-based transport layer communication protocol. TCP Header Structure tcp headertcp header T..."},"headers":[{"level":3,"title":"TCP","slug":"tcp","link":"#tcp","children":[{"level":4,"title":"TCP Header Structure","slug":"tcp-header-structure","link":"#tcp-header-structure","children":[]},{"level":4,"title":"TCP State Machine","slug":"tcp-state-machine","link":"#tcp-state-machine","children":[]},{"level":4,"title":"TCP Three-Way Handshake","slug":"tcp-three-way-handshake","link":"#tcp-three-way-handshake","children":[]},{"level":4,"title":"How does TCP ensure the reliability of transmission?","slug":"how-does-tcp-ensure-the-reliability-of-transmission","link":"#how-does-tcp-ensure-the-reliability-of-transmission","children":[]}]},{"level":3,"title":"UDP","slug":"udp","link":"#udp","children":[{"level":4,"title":"Characteristics","slug":"characteristics","link":"#characteristics","children":[]},{"level":4,"title":"Structure","slug":"structure","link":"#structure","children":[]},{"level":4,"title":"Broadcast Types","slug":"broadcast-types","link":"#broadcast-types","children":[]}]},{"level":3,"title":"Common TCP/UDP Attacks","slug":"common-tcp-udp-attacks","link":"#common-tcp-udp-attacks","children":[]}],"git":{"createdTime":1707924994000,"updatedTime":1714915461000,"contributors":[{"name":"Ryan Su","email":"yuansu.china.work@gmail.com","commits":4}]},"readingTime":{"minutes":9.46,"words":2839},"filePathRelative":"en/guide/concepts/network/3-tcp-udp.md","localizedDate":"February 14, 2024","autoDesc":true,"excerpt":"<h3>TCP</h3>\\n<p>Transmission Control Protocol (TCP) is a connection-oriented, reliable, byte-stream-based transport layer communication protocol.</p>\\n<h4>TCP Header Structure</h4>\\n<figure><img src=\\"/assets/image/article/network/tcp_en.png\\" alt=\\"tcp header\\" tabindex=\\"0\\" loading=\\"lazy\\"><figcaption>tcp header</figcaption></figure>"}');export{R as comp,U as data};
